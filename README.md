# Natural Reset Life — Link-in-bio

Static link page for Instagram bio + short redirects.

## Files
- `index.html` — link page
- `links.json` — link list
- `_redirects` — short links (edit `/go` for each new post)

## Update workflow
1. Update `/go` in `_redirects`
2. Commit + push
3. Site updates automatically
